# Phase 2 leftovers — index

One slice per **new agent chat**. Do not mix slices. Markdown is the source
of truth: `roadmap.md` **Now** §2 and **Phase 2**.

Engine debt A–C has landed. Run [`sanity-landed.md`](sanity-landed.md)
before the first leftover if it has not been run since those slices.

| Order | Id | Prompt to copy | After |
|-------|----|----------------|-------|
| — | sanity | [`sanity-landed.md`](sanity-landed.md) | after debt; after every leftover |
| 1 | `p2-edit` | [`p2-edit.md`](p2-edit.md) | landed |
| 2 | `p2-calc` | [`p2-calc.md`](p2-calc.md) | start here |
| 3 | `p2-dist` | (write when opening) | after `p2-calc` |
| 4 | `p2-stat-mode` | (write when opening) | after `p2-dist` |
| 5 | `p2-eqn-linear` | (write when opening) | after `p2-stat-mode` |
| 6 | `p2-eqn-cubic` | (write when opening) | after linear |

Then `p4-packaging`. Phase 3 stays gated. LineIO / 99-byte / `:` are
**not** this list. Unshifted CALC **is** this list (`p2-calc`).

Copy **only** the slice file (everything below its line) into the new chat.
Chat markdown links often do nothing on Windows — use Ctrl+P or canvas
buttons.
---

Follow `docs/principles.md` and `roadmap.md` Now. Open the slice prompt
named in the table for this chat (`p2-calc.md`).
Do not start packaging or Phase 3.
