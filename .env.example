# This app needs no API keys or secrets to run.
#
# Optional dev-only toggle: set DISABLE_HMR=true to turn off Vite hot module
# replacement (useful to avoid flicker while an agent is editing files).
# DISABLE_HMR=true
